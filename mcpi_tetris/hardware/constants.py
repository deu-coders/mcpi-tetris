import os

PIN_UP = int(os.environ['PIN_UP'])
PIN_DOWN = int(os.environ['PIN_DOWN'])
PIN_LEFT = int(os.environ['PIN_LEFT'])
PIN_RIGHT = int(os.environ['PIN_RIGHT'])
PIN_LAND = int(os.environ['PIN_LAND'])

PIN_JOIN = int(os.environ['PIN_JOIN'])
PIN_LEAVE = int(os.environ['PIN_LEAVE'])
PIN_START = int(os.environ['PIN_START'])

PIN_BUZZER = int(os.environ['PIN_BUZZER'])

PIN_LED_R = int(os.environ['PIN_LED_R'])
PIN_LED_G = int(os.environ['PIN_LED_G'])
PIN_LED_B = int(os.environ['PIN_LED_B'])